# skillx

To install dependencies:

```bash
bun install
```

To build the library:

```bash
bun run build
```

To typecheck:

```bash
bun run typecheck
```

Build output is written to `dist/`:
- `dist/index.js`
- `dist/index.d.ts`
